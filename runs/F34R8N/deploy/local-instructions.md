# Local Docker Instructions
1. Build image: `docker build -t apple-sales-forecasting .`
2. Run container: `docker run -p 5000:5000 apple-sales-forecasting`
3. Access API at `http://localhost:5000/predict`
